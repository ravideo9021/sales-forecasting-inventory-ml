# Full dependencies for the ML pipeline (training, optimization, experiments).
# Every package here is actually imported somewhere in the codebase.
# The Streamlit dashboard alone needs only the slim set in requirements.txt.
#   pip install -r requirements-full.txt

# Core data & ML
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.1
xgboost>=2.0
lightgbm>=4.0          # second base learner for the XGBoost+LightGBM ensemble
statsmodels>=0.13

# Optional foundation-model baseline (Amazon Chronos) — NOT installed by default
# (torch is heavy). Enable with:  pip install chronos-forecasting torch
# chronos-forecasting>=1.4
# torch>=2.2

# Optimization & hyperparameter tuning
pulp>=2.6
optuna>=3.0

# Dashboard & plotting
streamlit>=1.40
plotly>=6.0
pyarrow>=15.0
matplotlib>=3.5
seaborn>=0.11

# Utilities
pyyaml>=6.0
joblib>=1.3
loguru>=0.6

# Tests
pytest>=7.0
