# Slim dependencies for the RetailIQ Streamlit dashboard (app.py).
# This is what Streamlit Community Cloud / Hugging Face Spaces installs.
# For the full ML pipeline (training, optimization, experiments) see requirements-full.txt.

streamlit>=1.40
pandas>=2.0
numpy>=1.24
plotly>=6.0
scipy>=1.10
pyyaml>=6.0
joblib>=1.3
xgboost>=2.0
pyarrow>=15.0
